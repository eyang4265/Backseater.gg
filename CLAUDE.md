## Hard rules

This section is owned by the user. Never add, edit, reword, reorder, or remove
anything in it — not to reflect a code change, not to keep `AGENTS.md` and
`CLAUDE.md` in sync, not as cleanup — unless the user explicitly asks for that
change in this section. Every other instruction in this file, including the
"update the implemented-features sections" rule below, stops at this heading.
If a hard rule conflicts with anything else in this file or with a request,
say so and follow the hard rule.

<!-- Add hard rules below this line. -->

### Embed interactions must never time out

Every embed button, dropdown, and other component interaction must be
acknowledged within Discord's interaction deadline, including before any slow
Riot API, chart-rendering, or database work.

### `/match` and `/livegame` are the announcements, not copies of them

Completed matches are one code path and live games are another, and the slash
command and the automatic announcement must each go through the same functions
in the same order.

Completed match — `announce.format_match` → `announce.build_announcement_embed`
→ `announce.MatchAnnouncementView` → `announce.remember_match_view_state`.
Callers: `/match` (`commands/match/match.py`), the match poller
(`tracker.poll_and_announce` → `announce.publish`), the guest poller
(`guest_tracker`), and `/selftest`.

Live game — `announce.format_live_game` → `announce.build_live_game_embed` →
`announce.LiveGameAnnouncementView` → `announce.remember_live_game_view_state`.
Callers: `/livegame` (`commands/player/profile.py`) and the live-game poller
(`tracker.poll_live_games_and_announce` → `announce.publish_live_games`).

Never write a second embed builder, view, dropdown, chart, or button set for
either surface. Never branch inside those functions on whether the caller is a
command or a poller. Never inline "just this one field" into a command handler.
Any change to layout, fields, dropdown options, charts, or buttons goes into the
shared function, so both surfaces change together in the same edit.

Where the two genuinely must differ, the difference is a field on the
announcement dataclass that the shared code reads — the way
`MatchAnnouncement.name_style` being `NameStyle.CHAMPION` drops the Players
option for anonymous guest announcements — never a forked call path. If a
change cannot be expressed that way, say so and stop rather than forking.

Do not start or restart the bot solely because files have changed. When explicitly asked to run the bot, first stop every existing bot process, then start it with system Python 3.9.6 using `/usr/bin/python3 main.py`; never run `main.py` from `.venv`.

When you add or change a public slash command, update the `/commands` command directory with its current description and usage instructions. Keep owner-only commands out of that directory.
Keep each public slash command in its own command module named after the command (for example, `/champ` belongs in `commands/champ.py`). Move reusable logic shared by multiple commands into a separate shared module.

For any new command that requires a player, default to the account linked to the invoking Discord user when no player is supplied. For commands that require a match, default to that account's most recent match. Player-oriented commands should provide an optional `server` option plus a single combined `username` option (League or Discord username) so callers can select another account when needed; resolve it through `shared.resolve_username`/`target_for`, never separate `summoner`/`user` options.

Default embed layout for tabular data should follow `/jungleproximity`'s pattern: separate inline `embed.add_field` columns (name/label, then each stat) so Discord aligns the values automatically, rather than single-line text with inline markdown or manual padding.

Every Discord embed field value must be 1,024 characters or fewer. When a
tabular result can exceed that limit, split it at row boundaries into multiple
fields or pages before sending or editing the embed.

Automatic match announcements and `/match` must always render the same embed layout, fields, toggles, and buttons; keep them using the same shared rendering code rather than diverging implementations. The same requirement applies to automatic live-game announcements and `/livegame`.

Whenever functionality is added, changed, or removed, update the implemented-
features sections in both `AGENTS.md` and `CLAUDE.md`, keep their development
instructions synchronized, and update all affected docstrings before finishing
the task.

## Implemented features

- The shared completed-match Items renderer recovers an ADC/BOTTOM player's latest timeline boot purchase when the final Match-V5 participant inventory no longer contains one; sale and undo events clear that fallback, while a bot-lane role-quest `ITEM_DESTROYED` event preserves the boot because Riot moves it into the dedicated role-quest slot. Any boot present in the final payload remains authoritative. Boot detection uses Data Dragon tags plus conservative fallback IDs, including current Gluttonous Greaves `3008` and Gunmetal Greaves `3172`, while regular Manamune `3004` remains a normal item; known boots stay visible with a name lookup or `🥾` fallback even when Data Dragon or a custom emoji is unavailable.
- `/champstats` waits for the complete Riot history and timeline-backed item data before sending its report; missing timelines are requested only for eligible games on the requested champion (deduplicated and filtered by queue, role, patch, duration, and remake status). Its breakdown select state stays synchronized with the rendered embed when switching back to Runes, and pagination is clamped when the report changes.
- `/champstats` also recovers an ADC/BOTTOM player's latest still-held timeline boot when final participant slots omit it, including sale/destroy/undo handling shared with completed-match item recovery.
- `/champstats` excludes matches of 15 minutes or less and includes an always-visible `No Boots` breakdown row for every role with its own W-L and win rate; ADC/BOTTOM rows are only counted as no-boot when the timeline confirms there is no still-held purchased boot, using the same metadata-aware boot classifier as final inventory. For 2026 bot-lane role quests, an ADC/BOTTOM boot `ITEM_DESTROYED` event is treated as the boot moving into the dedicated role-quest slot, while `ITEM_SOLD` still removes it. No-boot games are appended once per match to repository-root `boots.log` with age/end timestamp, champion-role, K-D-A/result, and match length.
- Confirmed `/champstats` no-boot games are appended once per match to the repository-root `boots.log` with match age, end timestamp, champion/role, K-D-A score line, result, and game length.

- Jungle proximity uses the blue-nexus/blue-red-buff/bot-Scuttle/red-blue-buff/red-nexus and blue-nexus/blue-blue-buff/top-Scuttle/red-red-buff/red-nexus routes to classify regions: Mid between them, Top above the latter, and Bottom below the former. Within 1,500 map units of the Top route it splits proximity between Top and Mid; within the same band of the Bottom route it splits between Mid and Bottom.

- `run_bot.command` stops prior instances of this bot, then starts one instance with `/usr/bin/python3`.
- Discord slash-command options must be ordered with every required option before every optional option; Discord rejects the entire bulk command sync otherwise, leaving existing commands with stale schemas. Keep a network-free registration test covering this invariant and verify the startup log contains `Synchronized application commands` after public command changes.

- Discord slash commands for profiles, live games using the shared registered-player live-game announcement renderer with reversible Flex/Solo rank toggles and a summoner-name/rank name-column toggle, completed-match announcement rendering through `/match` (also persisting its button state like automatic announcements do, so its controls survive a bot restart) with a Display dropdown (Players/Ranks/Rankings, the last only offered for Flex games) and a Chart dropdown (Damage Done/Gold Graph/Gold Difference Graph/Jungle Proximity/Damage Taken/Healing and Shielding/Vision Score) — the Chart dropdown's Jungle Proximity option swaps in just the grouped bar chart comparing both junglers' Top/Mid/Bottom scores at the 5/10/15-minute checkpoints (rendered through the same shared `jungle_proximity_render` module as the standalone `/jungleproximity` command) behind whichever Display columns are already on screen, without replacing them with the side-by-side jungler text columns `/jungleproximity` itself shows, and the Display dropdown's Items option shows each player's end-of-game item slots as icon-only emotes (all six core slots always shown, with a ⬛ placeholder for an empty one, then the trinket icon directly after — no separator between them) in blue/red name/items columns, keeping its own Chart dropdown and the base embed's win/loss side color rather than a fixed one — both Display and Chart keep whichever stat chart is currently on screen instead of resetting to the damage chart, including Items itself, which renders that chart behind its item columns rather than clearing the image — centered-dot-separated player labels/KDA that drop the KDA suffix while the rank-name column is shown, Arena queue 1750 (3v3) team-aware live and completed-match columns, filtered match history (game mode limited to Normal (Draft), Ranked Flex, Ranked Solo/Duo, Arena, and ARAM, and champion) with up to 10 filtered results and a displayed W/L score, `/champ` displays global-by-default live OP.GG champion tier, win, pick, ban, patch, skill order, grouped primary/secondary/shard rune rows as selected icon-only emotes with configured shard mappings (including movement speed, cooldown reduction, and Nimbus Cloak), starter/core/boot item builds as icon-only emotes with duplicate items preserved from OP.GG's per-item count fields and quantity badges, and 3–5 non-core situational suggestions drawn from fourth through sixth item options with Mejai's excluded, defaults to the most-picked available position, and provides buttons for other positions, `/coachless` provides Coachless.gg rune, summoner-spell, starter, first/second/third/fourth+ item, and boots WPA/purchase tables with ID-based emotes and display names, fetched directly from Coachless.gg's JSON API (no browser rendering) with ID-to-name resolution via Data Dragon, one cached fetch per champion/role, and a Bravery button surfacing the highest-WPA option per category among picks under 1% pick rate, ordinal match references 1–20 for match/timeline/self-test, `/jungleproximity` showing both junglers' side-by-side blended dwell-time/kill-involvement lane-proximity scores per checkpoint plus the same grouped bar chart `/match`'s Chart dropdown's Jungle Proximity option renders, match lists, OP.GG links, PUUID lookup, mastery, timelines, champion rotation, server status, and command discovery; each public slash command has its own command module.
- Laning-phase counter-pick statistics (`bot_app/lane_matchups/`), derived from Match-V5 timelines rather than win rate: `extract.py` builds one row per (match, TOP/MIDDLE/BOTTOM/UTILITY role) — JUNGLE is excluded, there is no lane — from a composite gold/XP/CS/solo-kill-diff score at 14:00 (UTILITY instead uses the bot-lane *pair's* combined gold/XP lead plus bottom-half kill-participation, since a support's own CS/gold are near-meaningless); `match_cache.py` stores only sufficient statistics per `(patch, position, champion, opponent)` pair (schema `user_version = 5`, tables `lane_matchups`/`lane_ratings`) with both matchup directions kept so antisymmetry is a stored fact, never per-game rows; `model.py` fits one additive lane rating per champion plus a shared side (blue/red) coefficient by alternating projection (no scipy), derives each matchup's residual "effect" beyond rating and side, forces `effect(A, B) == -effect(B, A)` by construction, and shrinks noisy pair effects toward the rating-difference fallback via an empirical-Bayes constant estimated from the corpus itself; fitted role models are reused until a manual corpus write advances the in-process statistics revision; `collect.py` is a manually-triggered, unwired-into-startup snowball harvester (League-V4 apex seeding → puuids → matches → participants → repeat, deduped against `matches.sqlite`) that shares the tracker's own rate-limited `RiotClient`; there is no longer a slash command reading this data (`/counters` was removed) — the stored ratings currently feed `rating_baselines.py` collection only.
- `/laning [server] [username] [match_id]` (`bot_app/laning_render.py`, following `/jungleproximity`'s thin-command-wrapper pattern and mirroring its embed/chart shape) shows a player's Gold/XP/CS side by side with their lane opponent's — via the new `MatchTimeline.stats_at` — at the 5/10/15-minute checkpoints in jungleproximity-style inline embed columns (You/Opponent standing in for the two teams), plus a grouped Gold/XP bar chart per checkpoint colored by side (`build_laning_comparison_chart`, structurally identical to `build_jungle_proximity_comparison_chart` with metrics standing in for lanes); jungle has no lane opponent and is not supported.
- Account registry commands for tracking, owner-only account listing, refreshing, ownership reassignment, duplicate prevention, roster limits, and defaulting commands to the invoking user's linked account.
- Every riot-id lookup sweeps NA1, EUW1, and KR (`routing.lookup_platforms`): a `server` option on any player-oriented command only decides which of those platforms is checked first. Because account-v1 is regionally routed but answers for every region, the riot-id call alone cannot say where an account lives, so the platform is then settled by `RiotClient.home_platform`, a summoner-v4 probe across the same candidates — without it an EUW or KR player resolved through the americas route was labelled NA1 and every per-platform call (mastery, ranks, level) came back empty. `/add` sweeps and probes the same way, storing the platform that actually hosts the account. A tracked account's stored server still wins over all of it. Every user-facing "not found" message on a command with a `server` option names the platforms that were searched, via `shared.searched_servers` (`not_found_embed`, plus `/duo`'s own message), and echoes back every option the caller actually supplied via `shared.supplied_options_text` — a **You entered:** trailer listing each filled-in option (user-typed options rendered as a non-pinging mention, blank ones omitted), or a note that nothing was supplied and the caller's own linked account was used, so a typo'd tag or a stale `username` option is visible in the failure itself. Player-oriented commands take a single `username` option instead of separate `summoner`/`user` options: a Discord mention (`<@id>`) is looked up as a tracked account directly via `shared.resolve_username`, and anything else is treated as a riot id/summoner name — so either kind of value works in the one field. Server options that select a region rather than look up a player (`/champ`, `/rotation`) are unaffected.
- Automatic ranked-match announcements with LP changes, rank information and win rates for Solo/Duo rank displays, team columns, tracked-player-highlighted damage-dealt, timeline-based team gold-difference, damage-taken, healing-and-shielding, and vision-score charts, chronological processing, durable deduplication, and a Discord live-updating relative timestamp ("ended X ago") next to the game duration. `/match` and automatic announcements share a Display dropdown (Players/Ranks/Rankings/Ratings/Items/Mastery — Rankings only offered for Flex games, switching to Solo/Duo standings); Mastery shows each player's end-of-game champion points and reuses mastery already fetched for the same player/champion in a preceding live-game announcement when available, otherwise fetching it on demand. The Chart dropdown (Damage Done/Gold Graph/Gold Difference Graph/Jungle Proximity/Damage Taken/Healing and Shielding/Vision Score) remains available in the player and mastery views.
- Automatic live-game announcements with lobby queue, duration, player/champion rows, blue/red team columns, win rates, inferred positions, a live-updating relative timestamp ("started X ago") next to the game duration, one announcement per shared lobby, and a persistent Display dropdown (also present on `/livegame`'s view) whose options choose whether the name column shows summoner names, rank standing, or mastery points on the champion each player is currently playing; a Flex lobby's dropdown offers separate Flex Rank and Solo Rank options in place of a single Ranks entry, replacing the earlier standalone Flex/Solo toggle buttons, so picking either queue's standings is just another Display choice. Live games never put ranks in a separate bottom block, and any rank option replaces the summoner-name column just like completed-match announcements.
- Guest announcements that remain anonymous, trigger only when Guest and CrispyPineapple are in the same game, and allow every queue when both are present. Their Display dropdown omits the Players option, since showing summoner names would break that anonymity. CrispyPineapple alone follows normal tracked-player behavior.
- Per-guild announcement-channel configuration with a mandatory global fallback, guild membership routing, conservative handling of transient Discord failures, and cached batch lookups.
- Rank history with persisted snapshots, LP attribution, promotion and season-reset handling, local-day summaries through `/today`, clean annotated line graphs through `/lpgraph` with compact rank/LP point labels (for example, `G 1` over `29LP`), and a paginated leaderboard.
- Champion statistics and duo records, including the `/duo` command (optionally filtered by `game_mode`, limited to Normal (Draft), Ranked Flex, Ranked Solo/Duo, Arena, and ARAM with the same autocomplete as `/matchhistory`), backed by a versioned SQLite match cache that excludes remakes, respects team membership, migrates older schemas, prunes expired records, and degrades safely when unavailable.
- Centralized Riot API access with platform and regional routing, bounded retries, shared rate limiting, bounded request fan-out, connection pooling, coalesced concurrent cache misses, and optional SQLite caching for immutable match and timeline payloads.
- Debug-level logging of Discord's `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset-After`, `X-RateLimit-Bucket`, and `Retry-After` response headers whenever any HTTP request is rate limited (429).
- A single-instance startup guard using an exclusive file lock (`bot.lock`) that exits the process immediately if another bot instance is already running, preventing concurrent processes from racing Discord's per-guild command-sync rate limit. All console/debug log output, including the rate-limit headers above, is also mirrored to `bot.log` for post-mortem inspection.
- A `sync_commands_enabled` setting (default true) that, when set to false, skips the slash-command sync on startup entirely — lets the bot keep tracking/announcing while a punished guild command-sync rate-limit bucket cools down, without repeatedly hitting that endpoint.
- Data Dragon champion metadata, aliases, emoji rendering, profile icons, map images, patch-aware caching, and internal-ID-to-display-name resolution.
- Timeline analysis for kill and death locations, solo kills, lane opponents, gold and XP differences, role-aware level caps, five-minute series, time-weighted kill/death bounty ledgers, proximity-checked objective presence, and minimap/chart rendering.
- Per-player match ratings in `bot_app/rating.py` (version 1.1, standard five-player Summoner's Rift only — ARAM, Arena, remakes, and malformed role layouts are left unrated), built only from fields the Match-V5 match and timeline payloads actually carry: no invented ward-quality, support-quest-timing, jungle-camp (as opposed to jungle-monster), isolated-death, or teamfight-presence signals. Normalization is corpus-relative rather than lobby-relative: gold/XP/CS checkpoint metrics are first expressed as a signed difference against the direct role opponent (the 10:00 level plus the 10:00→15:00 swing, not two near-duplicate levels), then every metric is standardized against a stored population mean and standard deviation for that player's own role, supplied by `bot_app/rating_baselines.py` from the `rating_baselines` table. That makes a score comparable across matches, lets a whole lobby be collectively good or bad, removes the systematic role bias lobby-wide z-scores produced (measured 1.22 points down to 0.03), and stops two-player metrics such as jungle-CS diff and the bot-lane duo diffs from collapsing to a magnitude-free ±1. Any `(role, metric)` the corpus has not yet seen at least 200 player-samples of falls back to the old lobby z-score for that metric alone, so a cold database still rates. Every accumulating total (healing and shielding, crowd control, bounties, steals, enemy jungle monsters) is expressed per minute, since a pooled corpus spans every game length. Economy uses a separate metric table per role family: lane roles (gold/XP/CS diff plus swing, GPM, CS/min), jungle (jungle-only CS diff plus swing, enemy jungle monsters per minute — never called a camp count), and support (bot-lane duo combined gold/XP diff plus swing, support GPM). Combat scores solo/outnumbered kills and multikills as two separately weighted metrics rather than one sum, which had let a single takedown count up to three times. Objectives credits epic-monster and structure participation only by explicit killer/assist credit, never by frame proximity. Discipline scores Riot's own time-weighted bounty surrendered, percentage of the match spent dead, and share of team deaths — no invented isolated/traded/objective-enabling death classification. Win/loss is deliberately excluded so the score measures individual play rather than the result; letter grades (S+ through F) and a Full/Limited confidence tag (Limited when the timeline couldn't be fetched, degrading every timeline-derived metric to a renormalised box-score-only rating with an explicit note) are assigned after scoring, with `SCORE_SPREAD` and `GRADE_THRESHOLDS` derived from the observed composite distribution instead of assumed (roughly S+ 2%, S 4%, A 9%, B 20%, C 39%, D 21%, F 6%). A Ratings option in `/match` and automatic match announcements' Display dropdown renders these scores as blue/red name/score/K-D-A columns, fetching the match's timeline on demand for the fuller metric set and falling back to the box-score-only rating if that fetch fails, carrying over whichever chart was selected and keeping its own Display/Chart dropdowns so the chart can still be changed and the view can be switched back to Players/Ranks/Rankings while ratings are shown.
- Rating population baselines in `bot_app/rating_baselines.py`, backed by the `rating_baselines` table (schema `user_version = 5`), which stores per `(patch, position, metric)` sufficient statistics only — sample count and running sums, never a per-game row and never player identity — pools them across the stored patch window, and withholds any baseline below the sample threshold or with no spread rather than dividing by a noisy denominator. Collection is manually triggered and costs no extra Riot requests: `lane_matchups.collect.harvest` records samples from the match/timeline pairs it already fetches, and `backfill_rating_baselines` rebuilds the box-score baselines from matches already in the cache, replacing each key's totals rather than adding to them so it is safe to re-run and leaves timeline-derived baselines untouched. Live announcements consume baselines but never write them, so no match is ever counted twice and a tracked player's own games cannot dominate the population they are scored against. Baselines age out on the same rolling patch window as the lane-matchup statistics.
- Shared pagination, consistent embeds, bot-mention latency replies with `/help` guidance, centralized invocation, completion-time, and exception diagnostics for every parsed slash command, including terminal-visible Coachless endpoint, status, stage, cache, and traceback diagnostics, ephemeral permission errors, deferred slow commands, mention-safe responses, owner-only self-tests, an owner-only `/msg` command that sends an arbitrary message as the bot to a chosen channel (defaulting to the invoking channel), an owner-only `/ownercommands` directory that lists every owner-only slash command by introspecting the bot's registered application commands for the `@commands.is_owner()` check (rather than a hand-maintained list, so it stays current as owner-only commands are added or removed), and explicit command registration.
- Match/live-game, `/champ`, and `/coachless` component buttons use stable persistent views restored from incrementally updated SQLite records during async startup after bot restarts, independently of slash-command sync (a failed `sync_commands()` call no longer prevents view restoration); the former full-file JSON state is imported backward-compatibly on first use rather than rewritten for every message. Buttons that perform Riot API or chart work acknowledge within Discord's interaction deadline before editing the embed; immutable timelines and bounded rendered-chart bytes are cached, unchanged chart attachments are preserved across display-only switches, and `/livegame` persists its state through the same path as `/match` and automatic announcements.
- Environment-first configuration with JSON fallback, validation for numeric and boolean settings, IANA timezone support, configurable polling, cache controls, logging, and clean startup/shutdown tasks. `log_level` (env `LOG_LEVEL`, default `INFO`) controls verbosity to both the console and `bot.log`; modules log their normal operations at `INFO` and finer-grained internal detail (per-item work, cache hits/misses, request/response bodies, branch decisions) at `DEBUG`, so raising `LOG_LEVEL` to `DEBUG` surfaces that detail in the terminal without code changes.
- Atomic JSON persistence for accounts and tracker state, bounded match and rank history, thread-safe read-modify-write operations, and cleanup when accounts are removed or reassigned.
- Network-free automated tests covering domain logic, pollers, routing, publishing, commands, persistence, migrations, caching, rate limiting, chart fallbacks, and startup error handling.
- `/help` shows a concise, categorized command guide with one purpose-only line per command and safely breaks long categories at command boundaries; `/help command:<name>` shows a focused description with separate Usage and Options fields, deriving its text from the same `_COMMANDS` entries and introspecting option descriptions and requirements from the registered `discord.SlashCommand` so the two views stay synchronized.
- Server-scoped champion statistics show the resolved Riot platform in the response title; other player-lookup commands no longer append a server footnote to their embeds.
- Player-lookup commands describe their `user` and `summoner` options to Discord as "Discord username" and "League username" (with variants like "…(defaults to you)" and "Primary…") instead of the prior "User"/"Game Name" wording; successful player views omit the resolved server, while not-found messages identify the platforms searched and repeat the caller's own inputs. `/mastery`'s paginator lets anyone click its page buttons, not just the invoking user, unlike other author-scoped paginated views.
- `/champstats` waits for the complete available Riot history and timeline-backed item data before sending its report, defaults to all available history when no patch is supplied, and merges the scan with the local 180-day completed-match cache. The report shows a player's champion record plus per-game-count win rates for selected keystones, other runes, legendary items only, and completed boots in a separate breakdown, with exact All Games/Ranked/Solo-Duo/Flex queue scopes, Top/Jungle/Mid/ADC/Support role filters, an optional full or major/minor patch filter, a `since_patch` option that expands that filter through newer patches, and the date/time of the oldest included match; its enabled-by-default Discord boolean `filter` option hides choices played in fewer than 1% of games or in 2 or fewer games (set it false to show all; choices at exactly 1% remain visible when they have at least 3 picks); Riot scan failures degrade to cached results, boot upgrades are recognized from case-insensitive metadata tags and League boot-name vocabulary for ADC results even when historical Data Dragon tags are incomplete, final items tolerate historical Data Dragon depth changes while excluding entries with an upgrade path, and its breakdown/pagination controls never expire.
- `/counterstats` merges the local completed-match cache with a paginated Riot history scan and shows a selected champion's win rate against every enemy champion with champion emotes, sorted by highest win rate; required champion and player-role options select the initial history, the initial table compares against all enemy roles, `filter:true` keeps only matchups above 1% usage, the five in-message role buttons switch enemy roles, matchup rows use Discord-safe pages with Previous/Next controls while retaining the selected queue and player, and its controls never expire.
