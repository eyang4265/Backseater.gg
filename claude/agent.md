# Agent instructions

Whenever a feature or behavior is added, changed, or removed, update the
`## Implemented features` section in `AGENTS.md` to accurately describe the
current capabilities before finishing the task. Update affected docstrings at
the same time so they accurately describe the current behavior, parameters,
return values, and side effects.
