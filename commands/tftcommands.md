# `/tftcommands`

Lists the public Teamfight Tactics slash commands currently registered by the bot. Owner-only commands are excluded.

Usage: `/tftcommands` (no options).

The list groups account, match, and guide commands. It uses registered descriptions, so added commands appear automatically under Other until categorized.
