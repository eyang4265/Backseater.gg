# `/patchnotes`

Shows the five newest entries from the bot's maintained patch notes, with dates and change summaries. It reads the notes file each time it runs, so newly documented changes appear immediately, including before a commit.

**Usage:** `/patchnotes` (no options)
